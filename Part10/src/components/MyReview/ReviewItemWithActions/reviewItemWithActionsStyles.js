import { StyleSheet } from 'react-native';

const reviewOItemWithActionsStyles = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
  },
  buttonContainer: {
    flex: 1,
    padding: 10,
  },
});

export default reviewOItemWithActionsStyles;
